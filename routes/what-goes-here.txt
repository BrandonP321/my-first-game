# any required data to be imported

# all routes in a function that is exported to be used in the app.js

module.exports = function(app) {
  app.get("/api/tables", function(req, res) {
    res.json(tableData);
  });

  app.post("/api/tables", function(req, res) {

  });

};


# can have separate files for unique groups of routes (api, html, etc.)